{
  "name": "@autobier/db",
  "version": "0.0.1",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:push": "drizzle-kit push:pg",
    "db:studio": "drizzle-kit studio",
    "db:generate": "drizzle-kit generate:pg",

    "build": "tsc"
  },
  "dependencies": {
    "@autobier/types": "^0.0.1"
  },
  "devDependencies": {
    "drizzle-kit": "^0.20.17"
  }
}
